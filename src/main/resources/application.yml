spring:
  datasource:
    username: root
    password: nayoeng0119
    url: jdbc:mysql://localhost:3306/2018011394?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 9000

mybatis:
  mapper-locations: classpath:mapper/*.xml    # mapper映射文件位置
  type-aliases-package: com.Dealpf.demo.Bean   # 实体类所在的位置